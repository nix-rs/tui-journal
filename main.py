import sqlite3
from configDatabase import add_entry, db, delete_entry, read_entry, update_entry
from datetime import date




def main():
    #try:
        #with sqlite3.connect('check.db') as conn:
            #db(conn)
            #data = ('tags', date.today(), date.today(), 'contentes')
            #add_entry(conn, data)
            #data = (
            #    't',
            #    '2024-11-03',
            #    'number two ',
            #    3
            #)
            #update_entry(conn, data)
            #r = read_entry(conn)
            #print(r)
            #delete_entry(conn, 1)
    #except sqlite3.Error as e:
        #print(e)











if __name__ == '__main__':
    main()
